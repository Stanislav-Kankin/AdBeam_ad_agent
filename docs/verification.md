# Проверка MVP — 16 сентября 2026

Проверено в локальной Windows-среде с Python 3.12 и в Linux Docker-контейнере.

| Проверка | Результат |
| --- | --- |
| pytest локально | 71 тест пройден |
| pytest внутри Docker | 71 тест пройден |
| Ruff check и format | Без ошибок, 46 Python-файлов |
| Alembic upgrade + check | Миграция применена, расхождений модели и схемы нет |
| pip check | Конфликтов зависимостей нет |
| Compose config | Конфигурация валидна без подстановки секретов |
| Docker runtime | Образ собран; mock-проверка и вопрос через инструменты завершились успешно |
| Пользователь контейнера | adbeam, UID 10001 |
| Конфигурация примера | 3 валидных клиента, красный/жёлтый/зелёный сценарии |
| git diff --check | Без ошибок пробелов |

Telegram проверен через aiogram Dispatcher и тестовую сессию: команды, сообщения,
выбор кнопками, владелец выбора, allowlist и фоновые задачи. Реальные сообщения
не отправлялись. HTTP-адаптеры проверены через httpx.MockTransport, включая
201/202/429 Директа, пагинацию, ошибки доступа, период/цели/семплирование Метрики,
агрегированную выручку Roistat и формат запроса/ответа DeepSeek SDK.

Реальные ключи в рабочем окружении отсутствовали. Баланс DeepSeek не расходовался.
Проверка API на настоящем клиенте, реальные Tool Calls DeepSeek, long polling
Telegram и запуск на целевом сервере требуют локальной настройки `.env`.
PostgreSQL поддержан адаптером и миграциями, но отдельный PostgreSQL-сервер
в этой проверке не запускался.
